# SayCal — 출시 전 공격 문서

> 기준 문서: `STATUS.md` (최종 업데이트: 2026-05-06)
> 역할: 출시 전에 이 앱이 왜 망할지 지목하는 시니어 기획자
> 원칙: 문서에 없는 것은 구현되지 않은 것으로 간주한다. 호의적 추측은 하지 않는다.

---

## 결론

SayCal의 실패 원인은 기능 부족이 아니다. **사용자가 한 번 잘못 저장된 일정을 경험하는 순간 앱을 삭제하게 만드는 구조**다.

현재 Golden Path는 "앱 진입 → 한 문장 발화/입력 → 추가 → 캘린더 반영"이다. 이 흐름에는 사용자의 확인, 수정, 실패 인지, 권한 복구, 저장 후 검증이 없다. 자연어 일정 앱에서 가장 중요한 것은 빠른 저장이 아니라 **틀리지 않았다는 확신**인데, `STATUS.md` 기준 현재 제품은 그 확신을 제공하지 않는다.

---

## 1. 메인 화면은 일정 저장기가 아니라 오작동 발사 버튼이다

- 화면/파일: `ContentView.swift`, `AddScheduleFeature.swift`
- 기능: 메인 UI, 텍스트 입력, 음성 입력, "추가" 버튼
- 문서 근거: `파싱 결과 미리보기/수정 없음`

현재 플로우는 사용자가 입력한 문장을 AI가 어떻게 해석했는지 보여주지 않고 바로 `CreateCalendarEventUseCase.swift`로 넘긴다. 이 구조에서는 아래 케이스가 전부 사용자 몰래 캘린더에 저장된다.

- "이번주 토요일"을 사용자가 기대한 날짜와 다르게 해석
- "오후 7시"를 07:00 또는 19:00으로 오해
- 장소를 제목으로 넣거나 제목을 장소로 분리 실패
- 날짜만 맞고 시간/장소가 누락
- 제목이 비어 기본값 "일정"으로 저장

캘린더 앱은 사용자의 하루를 관리하는 도구다. 잘못 저장된 일정 하나는 단순 버그가 아니라 신뢰 붕괴다. 지금 구조에서는 사용자가 앱을 신뢰할 방법이 없다.

필수 조치:

- `ContentView.swift`에 저장 전 확인 화면 또는 인라인 편집 영역 추가
- `ParsedSchedule`의 `title`, `date`, `time`, `location`을 사용자에게 노출
- 저장 버튼은 파싱 결과 확인 이후에만 활성화
- 잘못된 필드를 즉시 수정할 수 있어야 함

---

## 2. 실패해도 아무 말 없는 앱은 실사용에서 죽는다

- 화면/파일: `AddScheduleFeature.swift`
- 기능: `parseResponse(.failure)`, `calendarEventResponse(.failure)`
- 문서 근거: `에러 피드백 부재`

`STATUS.md`는 실패 시 `isLoading = false`만 처리한다고 적고 있다. 사용자는 아래 상황을 구분할 수 없다.

- AI 파싱 실패
- 캘린더 권한 없음
- 캘린더 저장 실패
- 선택한 캘린더가 사라짐
- 음성 인식 실패
- 기기/OS가 FoundationModels 요구 조건을 만족하지 않음

이건 UX 문제가 아니라 제품 생존 문제다. 사용자가 "추가"를 눌렀는데 아무 일도 안 일어나면, 두 번째 시도보다 삭제가 먼저다.

필수 조치:

- `AddScheduleFeature.State`에 사용자 표시용 에러 상태 추가
- `ContentView.swift`에 실패 토스트/알림/배너 추가
- 파싱 실패, 저장 실패, 권한 실패를 서로 다른 문구로 분리
- 실패 후 사용자가 할 수 있는 다음 행동을 버튼으로 제공

---

## 3. 권한 거부 플로우가 없으면 첫 실행에서 이탈한다

- 화면/파일: `ContentView.swift`, `SpeechRecognitionUseCase.swift`, `CreateCalendarEventUseCase.swift`
- 기능: 캘린더 권한, 음성 인식 권한, 마이크 권한
- 문서 근거: `권한 거부 플로우 없음`

이 앱은 캘린더, 음성 인식, 마이크 권한 중 하나라도 막히면 핵심 가치가 무너진다. 그런데 권한 거부 시 안내, 설정 이동, 대체 입력 흐름이 문서에 없다.

특히 음성 우선 앱이라고 주장하면서 권한 거부 후의 화면이 정의되지 않았다. 사용자가 마이크를 거부하면 텍스트 입력만 남는데, 그 상태를 앱이 설명하지 않으면 "앱이 고장났다"고 판단한다.

필수 조치:

- `ContentView.swift`에 권한 상태별 안내 UI 추가
- 캘린더 권한 거부 시 설정 앱 이동 버튼 제공
- 음성/마이크 권한 거부 시 텍스트 입력 모드로 명확히 전환
- 권한 요청 타이밍을 첫 실행 온보딩 또는 기능 사용 시점으로 설계

---

## 4. "종료 시간 1시간 고정"은 캘린더 앱으로서 치명적이다

- 파일: `ParseScheduleUseCase.swift`, `CreateCalendarEventUseCase.swift`, `AddScheduleFeature.swift`
- 기능: 일정 시간 파싱 및 이벤트 생성
- 문서 근거: `종료 시간 고정 1시간`

일정은 시작 시간만으로 완성되지 않는다. "30분 면담", "2시간 회의", "저녁 약속", "하루 종일 워크숍"은 서로 다른 일정이다. 현재 문서 기준으로는 시간 없는 경우 종일 이벤트, 시간 있는 경우 1시간 고정으로 보인다.

이러면 사용자는 결국 캘린더 앱을 다시 열어 수정해야 한다. SayCal의 가치 제안인 "마찰 제거"가 깨진다. 앱이 한 번 저장하고, 사용자가 캘린더 앱에서 다시 고치는 구조는 자동화가 아니라 번거로운 우회로다.

필수 조치:

- `ParsedSchedule`에 `durationMinutes` 또는 `endTime` 추가
- `ParseScheduleUseCase.swift`에서 기간 표현 파싱
- `ContentView.swift`의 확인 화면에서 종료 시간 수정 가능하게 구성
- 기간이 없을 때 기본값 1시간을 사용자에게 노출

---

## 5. 반복 일정 미지원은 자연어 입력 앱의 핵심 기대를 배신한다

- 파일: `ParseScheduleUseCase.swift`, `CreateCalendarEventUseCase.swift`
- 기능: 반복 일정
- 문서 근거: `반복 일정 미지원`

"매주 월요일 9시", "격주 금요일", "매달 1일"은 자연어 일정 입력에서 매우 평범한 입력이다. 현재 문서 기준 이 입력은 단발 이벤트로 처리된다.

이건 엣지 케이스가 아니다. 직장인, 학생, 부모 타깃 모두 반복 일정을 자주 만든다. 반복 일정을 단발로 저장하는 앱은 조용히 틀린 앱이다.

필수 조치:

- `ParsedSchedule`에 반복 규칙 필드 추가
- `CreateCalendarEventUseCase.swift`에서 `EKRecurrenceRule` 생성
- 확인 화면에서 반복 여부를 명시적으로 표시
- 반복 파싱 실패 시 단발 저장 전에 사용자 확인 필요

---

## 6. 알람/리마인더가 없으면 "일정 추가"가 반쪽짜리다

- 파일: `ParseScheduleUseCase.swift`, `CreateCalendarEventUseCase.swift`
- 기능: 알람/리마인더
- 문서 근거: `알람/리마인더 설정 불가`

사용자는 "내일 3시 회의 10분 전에 알려줘"라고 말한다. 현재 문서에는 알림 시간 파싱도, `EKAlarm` 생성도 없다.

캘린더에 저장만 되고 알림이 없으면 실제 사용자는 중요한 일정을 놓친다. 이 앱은 시간을 줄이려다 사용자의 리스크를 키운다.

필수 조치:

- `ParsedSchedule`에 `alarmOffsetMinutes` 추가
- `ParseScheduleUseCase.swift`에서 "10분 전", "1시간 전", "전날" 파싱
- `CreateCalendarEventUseCase.swift`에서 `EKAlarm` 추가
- 알림이 없는 경우 확인 화면에 "알림 없음"을 노출

---

## 7. 온보딩 부재는 권한 앱에서 치명적이다

- 화면/파일: 문서상 별도 온보딩 화면 없음
- 기능: 첫 실행 경험
- 문서 근거: `온보딩 부재`

SayCal은 첫 실행에서 사용자가 이해해야 할 것이 많다.

- 왜 캘린더 풀 액세스가 필요한가
- 왜 마이크와 음성 인식 권한이 필요한가
- 어떤 문장을 말해야 잘 동작하는가
- 저장 전에 무엇을 확인해야 하는가

그런데 문서상 첫 진입 화면은 메인 입력 화면뿐이다. 권한 설명 없이 시스템 팝업부터 뜨면 사용자는 거부할 가능성이 높다. 권한을 거부하면 이 앱은 핵심 기능을 잃는다.

필수 조치:

- 첫 실행 온보딩 화면 추가
- 권한 요청 전 이유 설명
- 예시 입력 3개 이상 제공
- 권한 거부 후 복구 경로 제공

---

## 8. 캘린더 선택 설정은 있지만 실패 케이스 설계가 없다

- 화면/파일: `CalendarSettingsView.swift`, `CalendarSettingsFeature.swift`, `FetchCalendarsUseCase.swift`, `CreateCalendarEventUseCase.swift`
- 기능: 캘린더 선택 설정
- 문서 근거: `캘린더가 0개인 사용자 케이스 대응 방법은?`

캘린더 선택 기능을 추가했지만, 문서상 다음 케이스가 비어 있다.

- 캘린더가 0개
- 저장된 캘린더 ID가 더 이상 존재하지 않음
- 사용자가 읽기 전용 캘린더를 선택
- 기본 캘린더가 없음
- 캘린더 목록 조회 실패

설정 화면은 목록을 보여주는 것만으로 끝나지 않는다. 선택한 캘린더가 실제 저장 가능한지 검증해야 한다. 이 검증이 없으면 사용자는 "설정에서 선택했는데 저장이 안 된다"는 상태에 빠진다.

필수 조치:

- `FetchCalendarsUseCase.swift`에서 쓰기 가능한 캘린더만 노출
- 캘린더 0개 상태 UI 추가
- 저장된 ID가 유효하지 않으면 기본 캘린더로 fallback 또는 재선택 요구
- `CreateCalendarEventUseCase.swift` 저장 전에 선택 캘린더 유효성 검증

---

## 9. 다중 일정 입력을 방치하면 AI 앱으로 보이지 않는다

- 파일: `ParseScheduleUseCase.swift`, `AddScheduleFeature.swift`
- 기능: 자연어 파싱
- 문서 근거: `"일정"이 여러 개인 입력 처리 방향은?`

"내일 9시 회의, 11시 점심"은 사용자가 실제로 말할 법한 문장이다. 문서에는 이 입력을 어떻게 처리할지 없다.

현재 모델이 `ParsedSchedule` 단일 객체만 반환한다면, 둘 중 하나를 누락하거나 이상하게 합칠 가능성이 높다. 사용자는 하나의 문장에 여러 일정을 넣을 수 있다고 기대한다. 앱이 그 기대를 깨면 자연어 앱의 설득력이 사라진다.

필수 조치:

- 다중 일정 입력을 지원할지 거부할지 정책 결정
- 지원한다면 `[ParsedSchedule]` 구조로 변경
- 거부한다면 "한 번에 하나의 일정만 입력해 주세요" 메시지 표시
- 다중 일정 감지 시 자동 저장 금지

---

## 10. FoundationModels 의존성은 제품 리스크인데 fallback이 없다

- 파일: `ParseScheduleUseCase.swift`
- 기능: 온디바이스 AI 파싱
- 문서 근거: `파싱 실패 시 fallback 전략은?`

온디바이스 AI는 장점이지만, 제품 관점에서는 실패면이 넓다. 문서에는 FoundationModels가 사용 불가능하거나 파싱에 실패했을 때의 대체 전략이 없다.

가능한 실패:

- 기기/OS 조건 미충족
- 모델 응답 실패
- 날짜/시간 누락
- 한국어 상대 날짜 해석 오류
- 모델 출력이 `@Generable` 기대 구조와 불일치

fallback이 없으면 앱의 핵심 기능은 모델 상태에 종속된다. 일정 앱은 "가끔 안 되는" 순간 바로 버려진다.

필수 조치:

- 날짜/시간 정규식 fallback 추가
- 파싱 신뢰도 또는 필수 필드 누락 검사 추가
- 필수 필드 누락 시 저장 전 사용자 입력 요구
- FoundationModels 사용 불가 상태를 별도 에러로 노출

---

## 11. 앱 안에서 저장 결과를 확인할 수 없다

- 화면/파일: 문서상 일정 목록 화면 없음
- 기능: 저장 후 확인, 재편집
- 문서 근거: `일정 목록 화면 없음`

사용자는 방금 추가한 일정이 맞게 들어갔는지 확인하고 싶다. 현재 문서 기준 앱 안에는 추가한 이벤트 목록도, 상세 화면도, 재편집 기능도 없다.

결국 사용자는 캘린더 앱으로 이동해야 한다. SayCal이 해결하겠다는 "캘린더 앱의 다단계 입력 마찰"이 저장 직후 다시 등장한다.

필수 조치:

- 저장 성공 후 생성된 이벤트 요약 표시
- 캘린더 앱에서 열기 버튼 제공
- 최소한 최근 추가한 일정 목록 제공
- 잘못 저장한 이벤트 삭제 또는 수정 진입점 제공

---

## 12. 음성 우선이라고 하지만 음성 UX가 정의되지 않았다

- 화면/파일: `ContentView.swift`, `SpeechRecognitionUseCase.swift`
- 기능: 실시간 STT, ko-KR `SFSpeechRecognizer`
- 문서 근거: `partial result 스트리밍`

음성 인식이 된다는 것과 음성 UX가 완성됐다는 것은 다르다. 문서에는 아래가 없다.

- 녹음 시작/중지 상태 표시
- 사용자가 말하는 중인지, 인식 중인지, 파싱 중인지 구분
- partial result가 최종 텍스트로 확정되는 타이밍
- 음성 인식 실패 후 재시도
- 운전/이동 중 한 손 조작 시나리오

음성 우선 앱에서 마이크 버튼이 불안정하면 제품 전체가 불안정하게 느껴진다.

필수 조치:

- `ContentView.swift`에 녹음 상태별 UI 추가
- `SpeechRecognitionUseCase.swift` 실패/종료 이벤트를 Reducer로 전달
- partial/final transcription 상태 분리
- 음성 입력 중 "추가" 버튼 동작 정책 정의

---

## 13. 다국어 상태는 선언만 있고 제품화가 없다

- 파일: `ContentView.swift`, `CalendarSettingsView.swift`, 전체 SwiftUI 문자열
- 기능: 다국어
- 문서 근거: `다국어 카피 미분리`

개발 리전은 `en`, 실제 카피는 한국어 하드코딩이라고 적혀 있다. 이 상태는 다국어 지원이 아니라 다국어 부채다.

특히 App Store 메타데이터, 권한 문구, 시스템 설정 화면, 앱 내부 문구가 언어별로 어긋나면 사용자는 제품 완성도를 낮게 판단한다.

필수 조치:

- 모든 사용자 노출 문자열을 `Localizable.strings`로 분리
- 권한 문구도 한국어/영어 품질 검토
- 지원 언어를 한국어 전용으로 줄일지, 실제 영어까지 지원할지 결정

---

## 14. 테스트가 없으면 이 앱은 파싱 버그를 계속 재발시킨다

- 파일: `SayCalTests/SayCalTests.swift`, `AddScheduleFeature.swift`, `ParseScheduleUseCase.swift`, `CreateCalendarEventUseCase.swift`
- 기능: 테스트
- 문서 근거: `SayCalTests/SayCalTests.swift는 템플릿 그대로`

자연어 일정 앱은 입력 변형이 폭발한다. 테스트 없이 출시하면 아래 버그는 고칠 때마다 다시 생긴다.

- 이번주/다음주 계산 오류
- 자정/정오 처리 오류
- 시간 없는 일정의 종일 처리 오류
- 장소와 제목 분리 오류
- 권한 실패 시 상태 전이 오류
- 캘린더 저장 실패 시 UI 침묵

테스트 부재는 품질 문제가 아니라 운영 비용 폭탄이다. 특히 `STATUS.md`는 TCA 구조라 테스트 가능하다고 쓰면서 실제 테스트는 없다고 적고 있다. 가능한데 안 한 것이다.

필수 조치:

- `ParseScheduleUseCase` 날짜 계산 테스트
- `AddScheduleFeature` 성공/실패 Reducer 테스트
- `CreateCalendarEventUseCase` 캘린더 선택 fallback 테스트
- 권한 거부, 파싱 실패, 저장 실패 테스트

---

## 15. App Intents/Siri가 없으면 음성 우선 포지션이 약하다

- 파일/기능: 문서상 `App Intents` 없음
- 문서 근거: `위젯/단축어/Siri 진입점 없음`

음성 우선 앱이라고 말하면서 Siri, 단축어, 위젯 진입점이 없다. 사용자는 운전/이동 중에 앱 아이콘을 찾아 열고 마이크를 누르는 흐름을 원하지 않는다.

현재 Golden Path의 "앱 진입" 자체가 마찰이다. 음성 우선이라면 앱 밖에서 시작되어야 한다.

필수 조치:

- `App Intents`로 일정 추가 intent 제공
- 단축어에서 텍스트 입력을 받아 일정 생성
- 위젯 또는 Control Center 진입점 검토
- 앱 내부 음성 입력과 intent 입력의 파싱 로직 공유

---

## 출시 차단 기준

아래 항목이 해결되지 않으면 출시하면 안 된다.

1. 저장 전 파싱 결과 확인/수정 UI 없음
2. 파싱 실패/저장 실패/권한 실패 사용자 피드백 없음
3. 권한 거부 후 복구 플로우 없음
4. 선택 캘린더 유효성 검증 없음
5. duration/반복/알림 중 최소 하나도 제품적으로 처리되지 않음
6. `AddScheduleFeature` 주요 상태 전이에 대한 테스트 없음
7. `ParseScheduleUseCase` 날짜/시간 파싱 테스트 없음

---

## 가장 먼저 고쳐야 할 순서

1. `ContentView.swift`: 파싱 결과 확인/수정 화면 추가
2. `AddScheduleFeature.swift`: 실패 상태와 사용자 메시지 상태 추가
3. `CreateCalendarEventUseCase.swift`: 선택 캘린더 유효성 검증과 저장 실패 이유 분리
4. `ParseScheduleUseCase.swift`: duration, 반복, 알림 중 최소 duration부터 구조 확장
5. `SayCalTests/SayCalTests.swift`: 템플릿 제거, Reducer/UseCase 테스트 추가

---

## 최종 판정

현재 문서 기준 SayCal은 "빠르게 일정이 추가되는 앱"이 아니라 **틀릴 수 있는 일정을 확인 없이 캘린더에 쓰는 앱**이다.

출시 전에 해결해야 할 핵심은 기능 추가가 아니다. 저장 전 신뢰, 실패 시 설명, 권한 거부 복구, 캘린더 저장 검증이다. 이 네 가지가 없으면 사용자는 첫 실패에서 앱을 버린다.
